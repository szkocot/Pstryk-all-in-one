# Minimal config without default_config to avoid discovery
homeassistant:
  name: Home Assistant
  latitude: 52.0
  longitude: 19.0
  elevation: 0
  unit_system: metric
  time_zone: Europe/Warsaw

config:
frontend:
history:
logbook:
sun:
system_health:

logger:
  default: info
  logs:
    custom_components.pstryk_aio: debug